default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: central_meme
  schema_dump: schema.sql # rails 7 only

test:
  <<: *default
  database: central_meme_test
